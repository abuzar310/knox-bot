import type { KnoxModule } from "../../types.js";

const module: KnoxModule = {
  id: "gaming",
  name: "Gaming",
  description: "Roles, LFG, queues (coming later)",
  defaultEnabled: false,
  commands: [],
};

export default module;
