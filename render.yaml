# Knox — Render Blueprint (singapore)

previews:
  generation: off

services:
  - type: web
    name: knox-bot
    runtime: node
    region: singapore
    plan: free
    buildCommand: npm install -g pnpm@10.15.0 && pnpm install --frozen-lockfile && pnpm --filter @knox/shared build && pnpm --filter @knox/config build && pnpm --filter @knox/db build && pnpm --filter @knox/bot build
    startCommand: node --dns-result-order=ipv4first --no-network-family-autoselection apps/bot/dist/index.js
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: "22.12.0"
      - key: NODE_ENV
        value: production
      - key: NODE_OPTIONS
        value: --max-old-space-size=460 --dns-result-order=ipv4first --no-network-family-autoselection
      - key: DATABASE_URL
        sync: false
      - key: DISCORD_TOKEN
        sync: false
      - key: DISCORD_CLIENT_ID
        sync: false
      - key: KNOX_BOT_URL
        value: "https://knox-bot-gdf2.onrender.com"
      - key: KNOX_WEB_URL
        value: "https://knox-web-gdf2.onrender.com"

  - type: web
    name: knox-web
    runtime: node
    region: singapore
    plan: free
    buildCommand: npm install -g pnpm@10.15.0 && pnpm install --frozen-lockfile && pnpm --filter @knox/shared build && pnpm --filter @knox/config build && pnpm --filter @knox/db build && pnpm --filter @knox/web build
    startCommand: pnpm --filter @knox/db migrate && pnpm --filter @knox/web exec next start --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: "22.12.0"
      - key: NODE_ENV
        value: production
      - key: NODE_OPTIONS
        value: --max-old-space-size=460
      - key: DATABASE_URL
        sync: false
      - key: AUTH_DISCORD_ID
        sync: false
      - key: AUTH_DISCORD_SECRET
        sync: false
      - key: AUTH_SECRET
        generateValue: true
      - key: AUTH_URL
        sync: false
      - key: AUTH_TRUST_HOST
        value: "true"
